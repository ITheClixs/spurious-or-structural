# Data inventory

## G0 status

No external market data has been accessed. Nothing about venue schemas,
symbols, timestamps, coverage, gaps, historical format changes, file sizes, or
access restrictions is currently verified.

`data/manifest.json` is the machine-readable source of truth and records zero
downloaded bytes. The deterministic demo data are generated software-smoke
records and are neither market data nor a model of market data.

The first permitted byte-level discovery access is defined in
`PREREGISTRATION.md` and remains locked until G3.
