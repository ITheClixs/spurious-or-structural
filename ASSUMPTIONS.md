# Assumption register

Statuses are `untested`, `tested`, or `violated`. A violation that the design
depends on triggers the operating brief's escalation rule.

| ID | Assumption | Status | Test | What breaks if false |
| --- | --- | --- | --- | --- |
| A001 | Required compressed inputs average at most 150 MB per symbol-day over the eventual universe. | untested | G3 byte inventory across high-volume, median, tail, and delisted contracts. | The 348.6 GB acquisition plan fails; sample must be redesigned before bulk access. |
| A002 | The minimal 250 ms feature basis compresses to at most 50 bytes per symbol-bin. | untested | G3 representative Parquet benchmark after lossless column pruning. | The 10.4 GB lake and 25 GB steady-state plan fail. |
| A003 | No single archive needed by the design exceeds 4 GB, or the source supports true streamed/range decompression. | untested | G3 `Content-Length`, archive, and decompressor tests. | The one-symbol-day transient-disk contract fails. |
| A004 | Free historical trades, multi-level depth, liquidation, and contract metadata exist with usable timestamps. | untested | G3 byte-level inventory. | The intended empirical design or venue is infeasible. |
| A005 | Candidate regimes generate differential, non-proportional variance shifts. | untested | G4 population Jacobian and G6 pilot contrast spectra. | Heteroskedasticity supplies no identifying variation. |
| A006 | Structural `Lambda` is stable across the selected regimes. | untested | Regime interaction/stability tests designed before G6. | The constant-parameter identification model is misspecified. |
| A007 | Published primary sources contain enough numerical information to calibrate G2's observable factor strength and flow collinearity without opening local tape. | tested | Opened CCZ, Capponi--Cont, Hasbrouck--Seppi, Benzaquen et al., and Takahashi; source audit plus three hostile reviews. The one-minute Capponi--Cont triple supports a narrow conditional positive test, but not a full structural tuple or symmetric market null. | The conditional G2 positive fixture would be inadmissible before G3. A premise-killing null remains unavailable without a sharp source-compatible bound. |
| A008 | Twelve independent training dates per regime can place the operating point inside the G4 power frontier. | untested | G4 finite-sample frontier with date-cluster dependence. | Intraday sample size cannot rescue weak event-level variation; sample or `N` must change. |
| A009 | Genuine forced-liquidation events with known size, side, and timestamp can be reserved for G7. | untested | G3 source inventory, without opening event outcomes. | The proposed real-event execution validation is unavailable. |
| A010 | Using 60% of cold throughput plus a 25% time margin is conservative for sustained M4 Air work. | untested | Pre-phase warm/cold benchmarks and RSS monitoring. | Phase wall budgets understate thermal throttling. |
| A011 | The G0 deterministic generator is byte-stable across two clean runs with the locked interpreter. | tested | Two clean M4 runs and hosted Linux CI run `29416847411` produced matching committed JSONL, summary, and `_SUCCESS` bytes. | A future interpreter/toolchain change must regenerate under the drift check before merging. |
| A012 | The G1 primitive-only formulas condition on `f`, `u`, `v`, and proxy noise having zero contemporaneous cross-covariances. | tested | G1 uses independently keyed Gaussian component streams; structural-equation, analytic-path, and direct-regression tests pass. Empirical applicability remains a later model diagnostic. | Additional covariance terms enter both probability limits; the G1 formulas remain conditional rather than empirically complete. |
| A013 | The frozen `N=30`, `K=3`, `T=10^7` G1 runner stays below 1.5 GB RSS and eight hours on the M4 Air. | tested | The distinct-seed benchmark projected 18.2589 seconds after the declared derating. The frozen run then generated all 100 shards in 8.239197838 cumulative seconds; its slowest shard took 0.099412500 seconds and maximum checkpoint RSS was 437,829,632 bytes. | The G1 compute design would have failed and required redesign before scientific use; it did not. |
| A014 | The analytic worst-case relative standard error `1.7930730e-4` makes a `10^-3` maximum relative discrepancy attainable without seed retries. | tested | The sole frozen-seed 10-million-row run reported all 1,800 coefficients. Maximum relative discrepancy was `5.639467093140219e-4`; all targets lay inside the named 95% Bonferroni intervals. | A discrepancy at or above `10^-3` would have failed G1 attempt 1 and required diagnosis without changing the seed or threshold; it did not. |
| A015 | A one-spike permutation-invariant covariance with isotropic residual eigenvalues is a transparent source-compatible G2 fixture, though not a model of the real lower spectrum. | untested | G2 permutation/rotation tests and later G3/G6 empirical residual-spectrum comparison. | G2 can support only a conditional maximum-entropy counterexample; it cannot generalize to the real covariance geometry. |
| A016 | An independent proxy with 95% reliability is a sufficiently strong no-strawman observed-factor control for the conditional positive G2 claim. | untested | G2 reliability frontier, oracle-reliability negative control, and hostile review. | Passage at 95% remains conditional; failure cannot kill the market premise because weaker proxy reliabilities remain source compatible. |
| A017 | A shared stationary AR(1) stress coefficient of 0.60 is conservative enough for G2 finite-sample licensing without being treated as empirical calibration. | untested | Exact G2 size/power validation at 0.60 plus IID diagnostic; empirical serial dependence is deferred to G3/G6. | The G2 finite-sample power license may not transfer to more persistent or differently filtered market data. |
| A018 | The exact `CI_I` recovery plus six-specification CCZ reconstruction, including 15,195,600 validation and 45,586,800 research LASSO solutions, fit the 12-hour validation and three-hour research expected caps as well as the 24-/six-hour runtime stops. | untested | Fourteen-kernel distinct resource stream: one cold bundle, at least four total bundles, at least 600 post-cold seconds, and last-three-complete-bundle warm throughput, with every count mapped through the frozen derating/margin rule and total expected projection at most 16 hours. | The published-opponent contract is not executable on the target machine; S0004 fails before validation rather than silently reducing dates, folds, or lambdas. |
| A019 | Under independent equal-variance level errors calibrated to an 89.06% ten-level PC1 share, the population L1-normalized integrated score is `q` plus average error of exact variance `547/39530`, represented in binary64 as `0.013837591702504428`. | tested | Independent exact-rational and modal derivations, direct 30-by-30 reconstruction, and the implemented all-17-cell moment oracle; covariance PC share matched within `5e-16`, the closed-form measured coefficient within `1.93e-16`, and independent DGP re-review found maximum algebra discrepancy `0.0`. | The sealed observable targets and recovery attenuation would be wrong; S0004 could not execute. |
| A020 | A nine-node proxy-noise-amplitude grid is a useful finite-sample null stress, but it is not a uniform-size proof over the continuum. | untested | G2 reports all 459 component outcomes, the frozen maximum amplitude gap `0.015038828627620739`, maximum reliability gap `0.003307437435413063`, all cellwise gaps, and the family-union Clopper--Pearson interval; no continuum language is permitted. | Failure of the grid blocks S0004; passage still supports only the finite-grid calibration and cannot license a continuum-size claim. |
| A021 | The frozen CCZ `CI_I` reconstruction can recover a true `0.0046` focal cross coefficient under the full-size upper-endpoint flow, noisy-level, modal-return-noise, and AR law when only price confounding is removed. | untested | One predeclared 252-date counterfactual panel uses the same upper-endpoint distribution and deterministic maps except `Gamma=0`, but its own disjoint phase-25/scenario-4 addressed normals so validation never accesses the research realization: all 31 truths inside Bonferroni date-bootstrap-normal intervals, all point errors strictly below 50%, and no focal material-bias declaration. This is not called a size/power license. | S0004 fails before the research seed; a published-protocol failure cannot be attributed to confounding when the same procedure cannot recover the nonzero object without confounding. |
| A022 | Per-transform hashing and the weak issuance registry are cheap enough that provenance enforcement does not break the frozen G2 resource projections or 4 GB memory ceiling. | untested | The complete 48-date test-seed path passed weak-registry cleanup and used 63,766,528 bytes maximum RSS in 0.65 seconds. The sole 252-date A019 path, which includes source/runtime hashing, checkpoint publication, reload, and fresh-process refit, used 178,864,128 bytes peak RSS, 18.907810209 seconds, and 9,183,232 allocated checkpoint bytes. These are partial feasibility observations, not the frozen fourteen-kernel workload. Resource admission still requires byte-token validation inside every relevant cold/warm kernel, peak RSS, and no registry growth across complete bundles. | Resource admission fails before any validation seed. Redesign the in-process capability boundary without weakening address/provenance checks or silently omitting hashing from projected work. |
| A023 | The exact preregistration-amendment-A006 target fingerprint reproduces the five frozen Gaussian KATs; this does not establish byte stability for every unseen tail input. | tested | Test seed 1729: all five M4 Gaussian hashes, universal 150,000-word level-noise PCG digest, exact call trace, Linux one-ULP diagnostic at index 60,328, and hosted fail-closed run `29455143418`. Registered authority must rerun the same preflight before construction and record the runtime fingerprint. NumPy's same-build/environment/machine policy supports, but does not expand, this finite evidence. | Any failed M4 preflight blocks all registered G2 access. Sparse KATs cannot guarantee universal replay; cross-platform replay requires a new preregistered portable-transform design and Linux output cannot be substituted. |
| A024 | The local G2 checkpoint writer process and checkpoint directory can be treated as the trusted origin boundary; unkeyed hashes provide integrity and identity consistency, not cryptographic origin authentication. | untested | The 86-test checkpoint suite and 26-test seed-1729 supervisor suite cover crash, torn/mixed/stale/malformed/tampered artifacts, transient-cap and load/write races, atomic evidence publication, symlinked-root and pre-import Make rejection, launcher-source binding, exact RNG-address receipts, anonymous-pipe child authority, pre-draw identity drift, exceptional process-group teardown, compound terminal-publication and cleanup-interrupt faults, and fresh-process zero-draw recovery. Hosted run `30349473867` passed the Linux `/proc/self/fd` anonymous-pipe branch. The sole A019 seed-9191 run then published checksum-consistent base/cell artifacts and reproduced all three coefficient hashes in a fresh process with zero RNG draws. This tests operation within the assumed origin boundary, not the boundary's trustworthiness: coordinated same-user payload+manifest+hash forgery is not testable without an external trust anchor and remains explicitly out of scope. | If local checkpoint origin is not trusted, restored panel authority is not defensible. The only honest alternatives are an external signing root or full upstream replay, both of which require redesign before registered execution. |
